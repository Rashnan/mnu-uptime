Updating the CI key:
. account -> developer settings -> fine grained tokens -> find the token and regenerate it. make it long.
. (if token has expired). same flow, copy new token -> repo settings -> security / actions -> repo secrets / GH_PAT -> edit and save it
